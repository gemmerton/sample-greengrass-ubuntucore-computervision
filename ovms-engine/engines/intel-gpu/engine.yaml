name: intel-gpu
description: OpenVINO Model Server optimised for Intel GPU inference
vendor: Intel
grade: stable

devices:
  allof:
    - type: cpu
      manufacturer-id: GenuineIntel
      architecture: amd64
    - type: gpu
      bus: pci
      vendor-id: "0x8086"

memory: 4G
disk-space: 2G

components:
  - ovms-gpu

configurations:
  target_device: GPU
